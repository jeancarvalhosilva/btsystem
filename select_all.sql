SELECT usuario.*, ROWID FROM usuario;
SELECT funcao.*, ROWID FROM funcao;
SELECT escolaridade.*, ROWID FROM escolaridade;
SELECT estado.*, ROWID FROM estado;
SELECT cidade.*, ROWID FROM cidade;
SELECT funcionario.*, ROWID FROM funcionario;
SELECT especie.*, ROWID FROM especie;
SELECT raca.*, ROWID FROM raca;
SELECT proprietario.*, ROWID FROM proprietario;
SELECT animal.*, ROWID FROM animal;
SELECT tipo_item.*, ROWID FROM tipo_item;
SELECT servico.*, ROWID FROM servico;
SELECT produto.*, ROWID FROM produto;
SELECT estoque.*, ROWID FROM estoque;
SELECT pedido.*, ROWID FROM pedido;
SELECT pedido_itens.*, ROWID FROM pedido_itens;
SELECT agendamento.*, ROWID FROM agendamento;
SELECT agendamento_itens.*, ROWID FROM agendamento_itens;